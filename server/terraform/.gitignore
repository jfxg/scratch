# Terraform state — contains secrets, never commit
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.d/
.terraform/
.terraform.lock.hcl

# Actual variable values — never commit
terraform.tfvars
origina.terraform.tfvars
*.tfvars
